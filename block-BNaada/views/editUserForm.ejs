<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=width, initial-scale=version">
    <title>Book Form</title>
    <link rel="stylesheet" href="/stylesheet/style.css">
</head>
<body>
    <header class="header flex">
        <a class="headerLeftBtn" href="/">Bookstore</a>
        <a class="headerRightBtn" href="/books">Back</a>
    </header>

    <main class="main flex-center">
        <div class="form-outer">
            <span class="form-title">Add Book</span>
            <form action="/users/<%= user._id %>" method="post">
                <input name="name" type="text" placeholder='Name' value="<%= user.name %>">
                <input name="age" type="number" placeholder="Age" value="<%= user.age %>">
                <input name="address" type="text" placeholder="Address" value="<%= user.address %>">
                <input name="mobile" type="number" placeholder="Mobile" value="<%= user.mobile %>">
                <button type="submit" class="submitForm">Add Book</button>
            </form>
        </div>
    </main>
</body>
</html>